{
  "suite": "paired-smoke-v1",
  "description": "Fixed multi-turn scenarios for the paired Jev-vs-plain-LLM smoke benchmark. Sandbox-only (no Lark, no network dependencies) so effects are verifiable and runs are reproducible; expectations are machine-checked per turn per lane. Categories map to the architecture's advantage model: read_nav_heavy and batch_repetitive should favor the Jev fast path (many text-free steps), multi_turn_continuity exercises ledger/cache persistence across turns, procedural is write-then-verify, and writing_heavy is the control expected to favor (or tie) the plain lane because every step authors text. New-file names come from the authoring LLM's first line (flat, no directories), so file checks match on name substrings.",
  "scenarios": [
    {
      "id": "read-nav-short",
      "category": "read_nav_heavy",
      "favor": "jev",
      "description": "Turn 1 seeds three files (write-heavy), turns 2-3 are pure READ_FILE navigation with short answers - the shape where direct Jev steps dominate.",
      "max_steps": 16,
      "turns": [
        {
          "goal": "Create three sandbox files: alpha.md containing exactly the word sunrise, beta.md containing exactly the word ocean, gamma.md containing exactly the word mountain.",
          "files": [
            {"name": "alpha", "contains": "sunrise"},
            {"name": "beta", "contains": "ocean"},
            {"name": "gamma", "contains": "mountain"}
          ]
        },
        {
          "goal": "Read alpha.md, beta.md and gamma.md, then answer with the three words on one comma-separated line and nothing else.",
          "answer_contains": ["sunrise", "ocean", "mountain"],
          "answer_max_chars": 80
        },
        {
          "goal": "Read gamma.md again and answer with just the word it contains.",
          "answer_contains": ["mountain"],
          "answer_max_chars": 40
        }
      ]
    },
    {
      "id": "batch-anomaly-scan",
      "category": "batch_repetitive",
      "favor": "jev",
      "description": "Iterate a known set of files and find the outlier; the per-step decision is mechanical once the list is known.",
      "max_steps": 24,
      "turns": [
        {
          "goal": "Create five files one.txt, two.txt, three.txt, four.txt, five.txt. Each contains exactly the word normal, except three.txt which contains exactly the word anomaly.",
          "files": [
            {"name": "one", "contains": "normal"},
            {"name": "three", "contains": "anomaly"}
          ]
        },
        {
          "goal": "Read all five of those files and answer with only the filename whose content differs from the others.",
          "answer_contains": ["three"],
          "answer_max_chars": 40
        }
      ]
    },
    {
      "id": "ledger-continuity",
      "category": "multi_turn_continuity",
      "favor": "jev",
      "description": "Facts must survive across turns via the persisted ledger and the Docker volume; turn 3 additionally requires the authoring model to use turn 2's read observation.",
      "max_steps": 12,
      "turns": [
        {
          "goal": "Create memo.txt containing exactly the passphrase harbor-2179.",
          "files": [{"name": "memo", "contains": "harbor-2179"}]
        },
        {
          "goal": "What passphrase does memo.txt hold? Answer with the passphrase only.",
          "answer_contains": ["harbor-2179"],
          "answer_max_chars": 40
        },
        {
          "goal": "Create report.md whose content is the passphrase from memo.txt written in reverse character order.",
          "files": [{"name": "report", "contains": "9712-robrah"}],
          "soft": ["9712-robrah"]
        }
      ]
    },
    {
      "id": "bash-verify-loop",
      "category": "procedural",
      "favor": "jev",
      "description": "Write then verify via bash, and re-verify in a later turn; steps alternate between one write and mechanical verification.",
      "max_steps": 12,
      "turns": [
        {
          "goal": "Create check.txt containing exactly probe-ok, then use one bash command to verify the file content equals probe-ok.",
          "files": [{"name": "check", "contains": "probe-ok"}]
        },
        {
          "goal": "Use bash to cat check.txt and answer with its exact content.",
          "answer_contains": ["probe-ok"],
          "answer_max_chars": 40
        }
      ]
    },
    {
      "id": "count-and-append",
      "category": "read_nav_heavy",
      "favor": "jev",
      "description": "Four turns over one evolving file: seed, count, append, recount. Turns 2 and 4 are read-then-answer with a numeric check.",
      "max_steps": 16,
      "turns": [
        {
          "goal": "Create monitor.txt with exactly four lines, one per line: error E1, error E2, ok, error E3.",
          "files": [{"name": "monitor", "contains": "error E3"}]
        },
        {
          "goal": "Read monitor.txt and answer with only the number of lines that start with error.",
          "answer_contains": ["3"],
          "answer_max_chars": 20
        },
        {
          "goal": "Append a fifth line error E4 to monitor.txt, keeping the first four lines unchanged.",
          "files": [{"name": "monitor", "contains": "error E4"}]
        },
        {
          "goal": "Read monitor.txt again and answer with only the new number of lines that start with error.",
          "answer_contains": ["4"],
          "answer_max_chars": 20
        }
      ]
    },
    {
      "id": "long-form-control",
      "category": "writing_heavy",
      "favor": "plain",
      "description": "Control scenario: long-form authoring in both turns. Every step is text-bearing, so the Jev lane pays Jev decisions on top of LLM authoring - the plain lane is expected to win or tie here, which keeps the suite honest.",
      "max_steps": 12,
      "turns": [
        {
          "goal": "Write a roughly 200-word product announcement draft for a developer CLI tool named JevLoop into a new file.",
          "any_file_contains": ["JevLoop"]
        },
        {
          "goal": "Read your draft file and write a new file containing a three-sentence summary of it.",
          "min_files": 2
        }
      ]
    }
  ]
}
